
// To summarize, this code defines a functional component called `PageError` that renders a `div` element with the text "Error".
// This component can be imported and used in other parts of the application.
function PageError() {
    return (
        <div> No Pages " Page_Error " </div>
    )
}

export{ PageError }
